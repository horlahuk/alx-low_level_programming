#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints its name, followed by a new line.
 * @argc: argument count
 * @argv: argument array
 * Return: int
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
}

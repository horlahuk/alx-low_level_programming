#include "main.h"
/**
 * _isupper - checks is parameter is an uppercase character
 * @c: input character
 * Return: 1 for success and 0 for failure
 */
int _isupper(int c)
{
	if (c >= 65 c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
